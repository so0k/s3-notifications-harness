# 2026-08-28 — TerraConstructs port (Option C) live run

`TestTcons` **PASS** (1195s). App `tcons/` consumes `terraconstructs@0.0.0.jsii.tgz` built from
`~/tcons/base-cfncompat` (branch `feat/cfncompat-custom-resource`, commits `444b1b5` core +
`5e8891e` storage): stack A `new Bucket` + `addEventNotification` with context key
`@terraconstructs/aws-s3:keepNotificationInImportedBucket`, stacks B/C `Bucket.fromBucketName`
+ `addEventNotification`. Every stack renders `Custom::S3BucketNotifications` via
`cfncompat_custom_resource` (`Managed: "false"`), handler singleton per stack, per-stack
`CustomResourceResponsesBucket`.

| Stage | result |
|---|---|
| deploy A → {a} | ✅ |
| deploy B → {a,b} | ✅ (b live after 7 warm-up probes) |
| deploy C → {a,b,c} | ✅ |
| re-deploy A → {a,b,c} | ✅ `No changes. Your infrastructure matches the configuration.` |
| destroy B → {a,c} | ✅ |
| cleanup | ✅ |

TerraConstructs' own integ target `bucket-notifications-cross-stack` could **not** run: its
driver uses OpenTofu and `registry.opentofu.org` has no `cdktn-io/cfncompat`
(`Failed to query available provider packages`). Needs the provider published to the OpenTofu
registry, or a `filesystem_mirror` for local runs.

Raw `go test` output for this run: `../../test-reports/tcons-green-20260828.log`.

## 2026-08-29 — TerraConstructs in-repo integ under OpenTofu

`cdktn-io/cfncompat` 0.2.0 became available on `registry.opentofu.org` (opentofu/registry #5338
provider, #5340 signing key — same GPG key `BC7401D68DBFB79A` as the HashiCorp registry).
`make bucket-notifications-cross-stack` in `~/tcons/base-cfncompat/integ/aws/storage` (tofu,
unmodified) → `TestBucketNotificationsCrossStack` **PASS** (472s). Raw log:
`test-reports/tcons-integ-tofu-green-20260829.log`. The OpenTofu registry gap is closed.
